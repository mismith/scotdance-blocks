Use semantic Tailwind tokens from `src/assets/main.css` (e.g. `bg-card`, `text-foreground`, `border-border`) — avoid using hardcoded colors like `bg-gray-50`.
Only use `cursor-pointer` for links — buttons and other interactive elements should use `cursor-default`.
